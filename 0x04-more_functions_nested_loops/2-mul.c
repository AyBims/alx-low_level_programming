#include <stdio.h>
#include "main.h"
/**
 * mul - multiplies two integers
 * @a: is a variable
 * @b: is a variable
 * Return: always 0 (success)
 */
int mul(int a, int b)
{
	int ans;

	ans = a * b;

		return (ans);
}
