#include <stdio.h>

/**
 * main - entry point
 * @argc: argument counter
 * @argv: argument value
 * Return: always 0 (Success)
 */
int main(int argc, char **argv)
{
	int i;

	for (i = 0; i < argc; i++)
	{
		printf("%s\n", arv[i]);
	}
	return (0);
}
