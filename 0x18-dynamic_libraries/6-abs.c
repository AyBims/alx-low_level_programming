#include "main.h"
/**
 *_abs - function that generates absolute value of a number
 *@n: is a number
 *Return: n if positive and -n if negative (success)
 */
int _abs(int n)
{
if (n < 1)
{
return (-n);
}
else
{
return (n);
}
}
